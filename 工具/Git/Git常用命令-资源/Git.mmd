Mind Map generated by NB MindMap plugin   
> __version__=`1.1`,showJumps=`true`
---

# Git常用命令

## 基本配置

### 用户配置

#### git config \-\-global user\.name “xxx”

### 邮箱配置

#### git config \-\-global user\.email “xxx@xxx\.com”

### 项目初始化

#### git init

### 配置信息查看

#### git config \-\-global \-\-list

### 命令帮助

#### git \[命令符\] \-\-help

### 查看提交历史记录

#### git log \-\-pretty=oneline
> align=`left`


#### git show \-s \-\-pretty=raw \[版本号03d297\]

### 查看代码状态

#### 查看远程库状态

##### git remote show \<remote\>

##### git ls\-remote \<remote\>

#### 查看本地代码状态

##### git status

### 修改远程库仓库名称

#### git remote rename \<oldName\> \<newName\>

### 删除远程仓库

#### git remote remove \<branch\_name\>

## 代码拉取

### 项目克隆

#### git clone \[url\]

#### git clone \<版本库的网址\> \<本地目录名\>

### 代码同步

#### 同步到本地仓库

##### git fetch \<remote\>

#### 同步所有区域包括工作区

##### git pull

#### 同步代码同时合并其他人的reset

##### git pull \-\-rebase

## 代码提交

### 提交暂存区

#### git add \<file\>

#### git add \.

### 提交本地库

#### git commit \-m ""

### 提交远程

#### git push \<remote\> \<branch\>

### 跟踪点提交

#### git push

### 本地分支提交远程分支

#### git push origin \<本地分支\>:\<远程分支\>

## 分支操作

### 创建分支

#### 创建分支

##### git branch

#### 创建分支并跟踪

### 合并分支

#### merge合并，origin/serverfix合并到当前

##### git merge origin/serverfix

#### rebase合并，变基到master分支

##### git rebase master

### 查看分支

#### 所有分支跟踪版本信息

##### git branch \-vv

#### 查看分析信息

##### git branch \-v

## 代码回退

### 本地

#### 回退到上一个版本

##### git checkout \-\- \<file\>

#### 从暂存区撤销

##### git reset HEAD \<file\> 

##### git rm \-\-cached \<file

#### 暂存区撤销、工作区全部删除

##### git rm \<file\>

#### 返回到分支所指向的前一个提交

##### git reset HEAD^ \-\-hard

### 远程

#### 远程删除文件

##### 需要push 提交变更

#### 远程撤销

##### 参考4\.9

###### git reset \-\-hard \<版本号\>

###### git revert HEAD

###### 拉取手动修改并push

## 标签      

### 查看

#### git tag \-l

#### git tag \-l  “v1\.8\*”

### 添加

#### 轻量标签

##### git tag v1\.4\-lw

#### 附注标签

##### git tag \-a v1\.4 \-m "my version 1\.4"

##### git tag \-a v1\.2 \[版本号9fceb02\]

### 提交

#### git push \<remote\> v1\.5 

#### git push \<remote\> \-\-tags 

### 拉取

#### git pull 自动拉去远程标签

### 删除标签

#### 本地

##### git tag \-d \<tagname\>

#### 远程

##### git push \<remote\> :refs/tags/\<tagname\>

##### git push origin \-\-delete \<tagname\>
